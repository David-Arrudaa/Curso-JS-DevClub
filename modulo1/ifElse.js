/* Controlador de fluxo

    -IF(Se) -> Faça isso
    -Else(Se não) -> Faça isso

Operador de comparação
    > maior que
    <menor que
    == igual que
*/
const notaDoAluno = 5
const notaCorte = 5

if (notaDoAluno > notaCorte) {
    console.log("Aprovado")
} else {
    console.log("Reprovado")
}